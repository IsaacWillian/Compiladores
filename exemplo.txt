LOAD:
    GROUP group1:(path),(path),(path);
    ONE image1:(path);
    	
PROCESS:
    threshold in group1  above 100; //imagens devem ser binarias
    filter <tipo_filtro> in image1;
   
    
SHOW:
   show in grid GROUP1;
   show image1;
   show in line GROUP1;
    
